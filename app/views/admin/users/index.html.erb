<%= link_to '新建', new_admin_user_path %>
<% @users.compact.each do |user| %>
<div> 
   <%= user.name  %>
    <%= link_to '修改', edit_admin_user_path(user.id) %>
    <%= link_to '删除', admin_user_path(user.id), :method => 'delete' %>
</div> 
<% end %>
