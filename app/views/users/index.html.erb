<%= link_to '新建', new_user_path %>
<% @users.compact.each do |user| %>
<div> 
   <%= user.name  %>
    <%= link_to '修改', edit_user_path(user.id) %>
    <%= link_to '删除', user_path(user.id), :method => 'delete' %>
</div> 
<% end %>
